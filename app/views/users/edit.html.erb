<%= form_for @user do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= render 'address_form' %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.submit 'Update Profile' %>
<% end %>
